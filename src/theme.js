import { purple } from "@mui/material/colors";
import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    primary: {
      main: purple[500],
    },
  },
});
export { theme };
